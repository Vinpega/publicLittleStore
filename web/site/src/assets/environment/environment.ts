import { EnvironmentConfig } from "src/app/models/core/environment";

export const environment: EnvironmentConfig = {
  production: false,
  baseUrl: "https://localhost:4200",
  baseUrlApi: "https://localhost:44364",
};
